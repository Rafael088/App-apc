
import { StyleSheet } from 'react-native';

export const styleBtnMain = StyleSheet.create({
    container: {
        width: 100,
        height: 30,
        borderRadius: 25,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
    },
  });