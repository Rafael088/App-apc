import { StyleSheet } from 'react-native';

export const styleHome = StyleSheet.create({
    container: {
        width: '100%',
        height: '100%',
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
    },
    body: {
        width: '100%',
        height: '83%',
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
    }
  });